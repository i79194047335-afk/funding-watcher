# CLAUDE.md — Funding Watcher

Гайд для Claude Code по этому проекту. Пользователь — новичок в коде и крипто-терминах, общается по-русски. Объясняй простыми словами, без жаргона; новые термины — расшифровывай. Цель — учить, а не только писать код.

## Что это за проект

Бот-наблюдатель за ставками фандинга (funding rate) на бирже Hyperliquid.
Смотрит перпы (бессрочные контракты) по всем монетам и подсвечивает те, где
перекос ставки большой — это кандидаты на market-neutral стратегию
(спот + противоположный шорт/лонг, чтобы собирать фандинг независимо от цены).

**Важно про деньги:** реальная цель не доход (на бюджете ~$200 это центы),
а навык + рабочий бот для портфолио/фриланса. На старте бот ничем не рискует —
он только смотрит и считает. Реальные сделки — позже и крошечные.

## Стратегия в одну фразу

Фандинг капает каждый час. `+` ставка → лонги платят шортам (собираем, будучи в шорте).
`−` → шорты платят лонгам (собираем в лонге). Бот помечает кандидата от
`MIN_FUNDING_PCT = 0.05`%/ч (порог «обратить внимание»); реальная сделка отбивает
комиссии примерно от **~0.1%/ч** — это выше, цифры подгоняем по наблюдению.
На мейджорах край сжат, живёт длинный хвост (мелкие/свежие монеты). Гонка ботов
(MEV/Solana/мосты/стейблы) — не наш профиль, бюджет и латентность не те.

Первую реальную сделку делаем только на сигнале со знаком `+` (хедж = купить спот +
шорт перп). На `−` нужно шортить спот — новичку на HL недоступно, такие пропускаем.

## Текущее состояние

- **Все 6 шагов закрыты. Бот работает 24/7 на VPS по cron** (каждые 15 минут).
  Ждём первый реальный сигнал, чтобы пройти Шаг 6 руками.
- `watcher.py` тянет фандинг + объём (24ч) + наличие спота по ≈230 перпам,
  печатает топ-20 и блок «реальные кандидаты», шлёт в Telegram только НОВЫХ.
- Git + GitHub: github.com/i79194047335-afk/funding-watcher
- `.env` (токен Telegram + chat_id) защищён через `.gitignore` — НИКОГДА не в git.

## План (6 шагов) — все выполнены

1. ✅ Папка проекта + LOG.md
2. ✅ Тянем данные с Hyperliquid (API, без ключей)
3. ✅ Фильтр по наличию спота + ликвидности (объём за 24ч)
4. ✅ Уведомления в Telegram (со ссылкой на перп)
5. ✅ Запуск на VPS 24/7 (cron) + защита от спама
6. ✅ Памятка по первой сделке (`STEP6_GUIDE.md`); сама сделка — по приходу сигнала

## Технические детали

- Python 3.10, библиотеки `requests`, `python-dotenv` (есть в системе; также `pandas`/`numpy`).
- API Hyperliquid: `POST https://api.hyperliquid.xyz/info`, без ключей и регистрации.
  - Перпы: `{"type": "metaAndAssetCtxs"}` → `data[0]["universe"]` (имена),
    `data[1]` (контексты: `funding` — ставка/час в долях, `dayNtlVlm` — объём 24ч в $).
  - Спот: `{"type": "spotMetaAndAssetCtxs"}`. Нативный спот против USDC есть лишь
    у ~8 из 230 перпов — это фильтр «есть чем хеджить».
- Файлы: `watcher.py` (логика), `telegram_notify.py` (отправка), `get_chat_id.py`
  (помощник найти chat_id), `STEP6_GUIDE.md` (памятка по сделке).
- Пороги в начале `watcher.py`: `MIN_FUNDING_PCT=0.05`, `MIN_VOLUME_USD=1_000_000`.
- Антиспам: уведомлённые монеты хранятся в `data/notified_state.json`.
- Запуск разово: `python3 watcher.py` из `/root/projects/funding-watcher`.
  24/7: cron `*/15 * * * * cd <проект> && /usr/bin/python3 watcher.py >> logs/watcher.log 2>&1`.
- Папки `logs/` и `data/` существуют, но в git не попадают.

## Рабочий цикл и правила

- Каждый шаг фиксируем в `LOG.md` (дата + что сделали + что дальше), затем
  `git add … && git commit && git push`. Коммиты подписываем по-русски, как в истории.
- Перед любым коммитом проверять глазами, что `.env` НЕ в списке `git status`.
- Двигаться по одному шагу, не убегать вперёд. Объяснять перед тем как делать.
- `CONTEXT.md` — это сохранённый транскрипт исходного чата (исследование арбитража
  + договорённости по проекту). Не код, а контекст; в git его держать не обязательно.
