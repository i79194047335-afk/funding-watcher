# Funding Watcher — LOG

## 2026-06-15
- Создал проект в /root/projects/funding-watcher
- Шаг 2 готов: watcher.py тянет фандинг с Hyperliquid (230 монет)
- Первый запуск ок. Рынок спокойный, топ = HEMI +0.0392%/час
- Понял: + = собираем шортом, − = собираем лонгом, порог входа ~0.1%/час
- Дальше: фильтр по наличию спота + ликвидности
## 2026-06-15 (продолжение)
- Подключил git + GitHub: github.com/i79194047335-afk/funding-watcher
- .gitignore защищает .env (секреты не утекают)
- Первый коммит и push прошли
- Дальше: Шаг 3 — фильтр по наличию спота + ликвидности

## 2026-06-15 (Шаг 3)
- Создал CLAUDE.md — гайд по проекту для ассистента
- watcher.py теперь тянет ещё объём за 24ч (ликвидность) и наличие спота на HL
- Добавил блок "реальные кандидаты": фандинг ≥ 0.05%/ч + объём ≥ $1M + есть спот
- Находка: нативный спот на Hyperliquid есть только у 8 из 230 перпов
  (PURR, HYPE, TRUMP, BERA, PUMP, MON, STABLE, AZTEC). Остальное хеджить негде на HL.
- Урок на живых данных: HEMI топ по фандингу, но объём $52K и нет спота — торговать нечем.
  Кандидатов сейчас 0 (рынок спокойный) — это нормально.
- Дальше: Шаг 4 — уведомления в Telegram, когда кандидат появляется

## 2026-06-15 (Шаг 4)
- Telegram-уведомления через Bot API (только requests, без python-telegram-bot)
- Новые файлы: telegram_notify.py (отправка), get_chat_id.py (помощник найти chat_id)
- .env: поля TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID (в git не попадают)
- watcher.py теперь шлёт сообщение, когда появляются реальные кандидаты
- Проверено: без токена код не падает, отправка просто пропускается
- TODO пользователю: создать бота у @BotFather, вставить токен, запустить get_chat_id.py
- Дальше: Шаг 5 — запуск на VPS 24/7 (по расписанию) + защита от спама уведомлений

## 2026-06-15 (Шаг 4.1)
- Тест Telegram прошёл: сообщение доходит, цепочка данные→фильтр→уведомление работает
- В уведомление добавил кликабельную ссылку на перп: app.hyperliquid.xyz/trade/МОНЕТА
  — из бота сразу переход на торговую страницу (спот там же через переключатель Perp/Spot)

## 2026-06-15 (Шаг 5)
- Защита от спама: помним уведомлённых в data/notified_state.json, шлём только про НОВЫХ
  (ушедший кандидат забывается → при возврате снова даст сигнал)
- telegram_notify.py: .env грузится из папки скрипта (работает при запуске из cron)
- Запуск 24/7 через cron, каждые 15 минут:
  */15 * * * * cd /root/projects/funding-watcher && /usr/bin/python3 watcher.py >> logs/watcher.log 2>&1
- Проверено: антиспам не дублирует, cron-команда отрабатывает, лог пишется в logs/watcher.log
- Проект, по сути, готов и работает. Дальше: Шаг 6 — первая реальная сделка по сигналу (руками)

## 2026-06-17 (правки после ревью DeepSeek + фидбек разработчика)
- Провели двухраундовое ревью кода. 8/8 пунктов DeepSeek подтверждены по исходникам.
- Правки 1+2 (сигнал на выход + защита от минусовых сигналов):
  - Терминал: кандидаты разделены на «доступны (+)» и «пропускаем (−, нужен шорт спота)»
  - Telegram: уведомления только по положительному фандингу (отрицательные недоступны без шорта спота)
  - Добавлен сигнал на выход: когда монета выходит из кандидатов → уведомление «пора закрывать»
  - notify_exited() — новая функция для оповещений о выходе
  - notified_state.json теперь отслеживает только actionable (положительные)
- Дальше: пункты 3-5 (два порога 0.05/0.10, try/except на API, история в CSV)
